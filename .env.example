# Session encryption key, min 32 chars. Generate: openssl rand -base64 32
NUXT_SESSION_PASSWORD=

# MySQL connection. Local DBngin: mysql://root@127.0.0.1:3306/freelance_tracker
# Hostinger: use host 127.0.0.1 (not the srvNNNN.hstgr.io remote host)
DATABASE_URL=
